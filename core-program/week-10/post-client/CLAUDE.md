# CLAUDE.md

The src/service/services.js serves as is a full implementation of the services.starter.js file. That last file is the file that trainees will complete as during the training session. Both files should not include the admin functions.