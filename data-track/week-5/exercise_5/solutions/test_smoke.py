def test_pipeline_imports():
    # TODO 1: Replace the placeholder below with a real import from your pipeline module.
    # WHY start with assert True: a smoke test's job is to confirm the test runner can
    # execute. Once CI is wired and green, you replace this with a real import or a
    # minimal behaviour check. Starting with a passing assertion means your first CI
    # run is green, and you can verify the failure in step 3 by breaking the test
    # intentionally — rather than debugging a broken environment at the same time.
    assert True


# Step 3 — breaking the test to confirm CI fails:
# Change the line above to `assert False` and push. Watch CI fail.
# Then revert to `assert True` and push again. CI goes green.
# This confirms that your workflow actually reports failures — not just that it runs.
