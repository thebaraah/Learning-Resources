"""Workshop 1 Part A starter.

TODO:
  1. Load .env with python-dotenv (`load_dotenv()`).
  2. Read INPUT_PATH and OUTPUT_PATH from os.environ.
  3. Raise ValueError with a clear message if either is missing.
  4. Read VAT_RATE as a float, default to 0.21 if absent.
"""

import os

from dotenv import load_dotenv

load_dotenv()

# TODO: replace these stubs.
INPUT_PATH = ...
OUTPUT_PATH = ...
VAT_RATE = ...
